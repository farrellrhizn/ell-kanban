# Frontend (React + Vite)

## Cara menjalankan

1. Salin `.env.example` menjadi `.env` lalu sesuaikan URL API bila perlu.
2. Install dependency React.
   ```bash
   npm install
   npm run dev
   ```
3. Akses `http://localhost:5173`.
